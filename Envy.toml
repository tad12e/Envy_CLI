# Envy.toml
#
# Optional project config for Envy.
# (Currently used as a project-root marker; CLI options still take precedence.)

[envy]
env_file = ".env"
schema_file = "envy.schema.json"
docs_output = "README_ENV.md"
sync_output_file = ".envy.sync"
entropy_threshold = 4.3
